# Server Configuration
PORT=5000

# Database Configuration
# Replace <username>, <password>, <cluster>, and <dbname> with your actual MongoDB credentials if using Atlas, or specify a local MongoDB URI.
MONGO_URI=mongodb://localhost:27017/decodelabs_db
